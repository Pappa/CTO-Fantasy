import Phaser from "phaser";

export class CreditsScene extends Phaser.Scene {
  constructor() {
    super("CreditsScene");
  }

  init() {}

  preload() {}

  create() {}

  update(time, delta) {}
}
